{
  "name": "prstudio-site",
  "private": true,
  "version": "1.0.0",
  "description": "prstudio.ch — build estático multilingue",
  "scripts": { "build": "node build.js", "test": "cd tests && npm test" },
  "devDependencies": { "csso": "^5.0.5", "terser": "^5.31.0" }
}
