# AUTOPILOT · TEST + FIX · 2026-09-02

## E2E (Playwright, Chromium, file://store/)
40/40 passed em 375×812 e 1366×900: sem erros JS, sem overflow horizontal, hreflang ×3, JSON-LD Product, toggle compra única/abo (preço + href com `selling_plan`), guarda de placeholder no checkout, troca DE→FR e persistência após reload, 54 slots i18n preenchidos, FAQ, 3 páginas legais com h1 e link de retorno, foco por teclado.

## Lighthouse mobile (http://localhost)
| Performance | Accessibility | Best practices | SEO |
|---|---|---|---|
| 90 | 100 | 96 | 100 |
Perdas: fontes Google render-blocking (540 ms) e cache/compressão (server-side, Netlify/Pages tratam).

## FIX aplicados
1. Radios do plano 18px → 24px; links do footer com min-height 32px (alvos de toque). Re-run: 40/40.
2. Fontes Google carregadas em `media="print" onload` (não bloqueantes) + fallback `<noscript>`.

## Pendente (exige loja Shopify criada pelo utilizador)
- Checkout real com TWINT / cartão em modo teste (Bogus Gateway), subscrição via Shopify Subscriptions.
- Substituir `VARIANT_ID_ONCE`, `VARIANT_ID_SUB`, `SELLING_PLAN_ID` em `store/js/store.js`.
- Emails automáticos (Shopify Email) e pixels, ver `autopilot/build/shopify-setup.md`.
